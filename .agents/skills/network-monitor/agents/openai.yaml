interface:
  display_name: "Network Monitor"
  short_description: "Repo-specific patterns and workflows for network-monitor"
  default_prompt: "Use the network-monitor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true